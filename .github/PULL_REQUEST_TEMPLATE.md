## Summary

## Checklist

- [ ] Tests added or updated (`test/fixtures` when lint/brief/write paths change)
- [ ] `pnpm test`, `pnpm typecheck`, `pnpm lint` pass
- [ ] Changeset added for user-facing changes (`pnpm changeset`)
- [ ] Docs / README updated if a command changed
